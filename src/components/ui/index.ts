/**
 * * All the components should be divided by their category. eg. ui, forms, home, about, contact.
 * * Each folder in the component should export all the other components.
 */
export { default as Layout } from './Layout';
